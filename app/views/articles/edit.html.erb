<h1>Edit article</h1>

<% if @article.errors.any? %>
  <h2>Following errors prevented article from being saved</h2>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with(model: @article, local: true) do |f| %>

  <div class="form-field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  
  <div class="form-field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <%= f.submit %>

<% end %>
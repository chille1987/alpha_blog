<div class="container">
  <h2><%= @user.username %></h2>
  <%= gravatar_for @user %>
  <%= render 'articles/article' %>
  <div class="flickr_pagination mb-4">
    <%= will_paginate @articles, :container => false %>
  </div>
</div>